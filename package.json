{
  "name": "tesla-landing-page-clone",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "sass": "sass --watch --no-source-map src/sass:src/css --style compressed"
  },
  "devDependencies": {
    "vite": "^6.0.5"
  }
}
